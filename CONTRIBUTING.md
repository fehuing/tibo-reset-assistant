# Contributing

欢迎通过 Issue 提交问题，或通过 Pull Request 改进页面、可访问性和采集适配器。

提交前运行 `npm test`、`npm run typecheck`、`npm run build` 和 `python scripts/smoke.py`。自然语言判定的修改请提供最小可复现输入，并说明预期状态；不要将测试推文写入真实的公告数据。

不要提交账号 cookie、API Key、SSH 密钥、数据库、真实投票、个人二维码或未脱敏日志。数据源适配应使用你有权限访问的公开内容或 API，不绕过访问限制。贡献的自有代码以本仓库 MIT 许可证提供。
