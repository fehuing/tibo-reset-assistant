# 更新日志

## 1.2.0 — 2026-09-13

- 同步 Codex CLI 分析管线：先采集公开正文、上下文和 X 原页面截图，再生成完整中文翻译、双语摘要及重置分析。AI 模式通过 `npm start -- --ai` 显式开启。
- 增加固定模型角色、JSON Schema、来源哈希和原文依据校验；无关动态只归档和翻译，个人重置不计入全局事件。模型只返回结果，由程序校验后更新数据。
- 增加模型队列、重试、超时与安全错误码；AI 调用失败保留已有数据，不自动切回旧规则判定。默认启动仍使用离线示例，旧 `--live` 模式保留。
- 公告改为简洁头像气泡，默认显示最新三条，其他折叠；全文、中文翻译和 X 原始截图保留在详情中，后台保留关联与判定数据。
- 新重置观察覆盖线索 / 预告到实际确认的空档期，确认或明确取消后关闭，不再按固定 24 小时自动结束。来源延迟时保留提示并暂停投票。
- 对齐已核实的旧日历数据，保留最近 26 周、北京时间布局；最近一次重置与公告发布时间分开展示，有核实时间时使用实际重置时间。
- 更新默认历史快照至 2026-09-13；保留历史来源时间，不引入在线站点的投票和求重置计数，也不覆盖已有实例数据。
- 新增自己的 Codex 账号接入指南，包括 CLI 安装、设备码登录、同系统用户运行、模型权限、网络设置、结构化结果和故障排查。说明模型请求使用云端和账号额度。
- 仓库联系方式补充公众号 aicodexxx 二维码；继续保留微信、小程序与抖音联系素材，不发布收款码，不自动向部署者网页添加作者二维码。

## 1.1.0 — 2026-09-07

- 新增采集诊断接口和首页提示，区分未开启、首次检查、网络/DNS/TLS 故障、X 拒绝访问、限流和解析失败。
- 公告接口附带采集状态，分别展示公告与回复采集的最近检查、最近成功；失败不覆盖旧数据，备用成功也不标为 X 直采成功。
- 新增任务长时间未更新的提示；公开错误只包含安全错误码，不返回代理凭据和原始异常。
- 在 README 显著说明 UI 参考 Codex Resets、业务代码独立实现，并保留第三方依赖的许可说明。
- 按作者授权在 README 加入小程序码、微信联系码、抖音主页链接和二维码；这些联系素材不自动展示在部署后的网页。
- 在仓库说明自愿支持原则，暂不开放打赏，不上传收款码。

## 1.0.0 — 2026-09-07

首个独立网页版开源版本。

- 带上中英文页面、深色模式、手机布局和重置历史日历。
- 同步公告状态、事件去重统计、完整正文、中文阅读和截图缩略图功能。
- 加入重置观察、回复上下文、独立社区投票和本地持久化计数。
- 提供公开 X 采集、截图归档、可选翻译的源代码。
- 增加一键安装启动、依赖锁文件、Docker、配置示例和自动测试。
- 将数据、图片和分享链接改为当前实例，默认使用有明确标识的历史快照。
- 移除线上环境耦合和个人二维码；二维码区域支持自行配置。
